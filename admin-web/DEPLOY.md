# Docs
/*.md
!/README.md

# Static site for admin.theloop-app.com
services: []

# Optional: use Cloudflare Pages / Netlify with build:
#   build command: npm ci && npm run build
#   publish: dist
#   env: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_API_URL=https://api.theloop-app.com
