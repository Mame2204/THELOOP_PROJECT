/**
 * Notes membres sur fiches contenu (emoji visages → `EstablishmentRatingPanel`).
 *
 * Produit (2026) : désactivé — curation THE LOOP en amont ; signaux perf = clics (curiosité)
 * + favoris (intention de garder). Le terme « moyenne×poids » dans le score admin reste
 * dans la formule mais vaut 0 tant qu’aucune note n’est collectée.
 *
 * Réactiver quand volume d’avis + règle métier (seuil min.) sont définis.
 */
export const MEMBER_ESTABLISHMENT_RATINGS_ENABLED = false;
