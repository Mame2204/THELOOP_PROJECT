/**
 * Achat PASS Prime — interrupteur UI uniquement (PWA).
 *
 * `false` : masque les liens /prime, /abonnement et les CTA « Devenir Prime ».
 * La logique métier (activation, simulation, pages) reste intacte.
 *
 * Pour réactiver : passer cette constante à `true`.
 */
export const PASS_PURCHASE_UI_ENABLED = false;
